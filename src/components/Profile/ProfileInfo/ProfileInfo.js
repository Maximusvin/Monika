import React from 'react';
import styles from './ProfileInfo.module.css';

const ProfileInfo = () => {
  return <div className={styles.description}>Ava + Description</div>;
};

export default ProfileInfo;
